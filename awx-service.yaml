apiVersion: v1
kind: Service
metadata:
  name: awx
  namespace: awx-namespace
  labels:
    service: ansible_awx_service
    
spec:
  selector:
    app: ansible-awx-service
  type: LoadBalancer
  # port mapping
  ports:
  - port: 80
    targetPort: 5000
